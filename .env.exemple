# Valores para o algoritmo base e o seu
VALOR_TARGET=990000000

TIME_TARGET=60  # Tempo alvo para execução (em segundos)

# Caminhos dos scripts (ajuste se necessario)
BASE_SCRIPT=base-lidiano.py
MEU_SCRIPT=main.py

# Afinidade de CPU
# O professor (BASE) e o seu (MEU) rodam no MESMO nucleo
CPU_BASE=0
CPU_MEU=0
# O MONITOR roda em um nucleo DEDICADO
CPU_MONITOR=1

# Configurações do Monitor
MONITOR_INTERVAL=0.1    # Intervalo de verificação em segundos
MONITOR_THRESHOLD=0.1   # % de variação para ajuste de nice
